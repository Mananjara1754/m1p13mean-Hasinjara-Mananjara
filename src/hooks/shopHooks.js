// Hooks for Shop model
// Currently empty as image handling is moved to Cloudinary and no local file manipulation is needed.

module.exports = (schema) => {
    // schema.post('save', moveLogo); // Removed
};
